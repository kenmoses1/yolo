apiVersion: v1
kind: Service
metadata:
  name: devops-group2-client-service
spec:
  selector:
    app: devops-group2-client
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
